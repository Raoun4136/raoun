export const consoleString = (value: string) => {
  console.log(`Console Log String : ${value}`);
};

export const consoleNumber = (value: number) => {
  console.log(`Console Log Number : ${value}`);
};

export const consoleBoolean = (value: boolean) => {
  console.log(`Console Log Boolean : ${value}`);
};

export const consoleArray = (value: any[]) => {
  console.log(`Console Log Array : ${value}`);
};

export const consoleObject = (value: any) => {
  console.log(`Console Log Object : ${value}`);
};
