{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "npmClient": "yarn",
      "prepare": { "lifecycle": { "build": "yarn run webpack" } }
    },
    "version": {
      "npmClient": "yarn",
      "conventionalCommits": true
    }
  }
}
